<%= form_with(model: profile_picture) do |form| %>
  <% if profile_picture.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(profile_picture.errors.count, "error") %> prohibited this profile_picture from being saved:</h2>

      <ul>
        <% profile_picture.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.file_field :image %>
  </div>

  <div>
    <%= form.label :profile_id, style: "display: block" %>
    <%= form.number_field :profile_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
