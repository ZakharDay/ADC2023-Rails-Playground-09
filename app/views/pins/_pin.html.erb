<div id="<%= dom_id pin %>">
  <%= image_tag pin.pin_image.url, width: 300 if pin.pin_image.url %>

  <%= pin.user.email %>

  <h2>
    <%= pin.title %>
  </h2>

  <p>
    <%= pin.description %>
  </p>

</div>
